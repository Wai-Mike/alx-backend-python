apiVersion: v1
kind: Service
metadata:
  name: messaging-app-service-bg
spec:
  selector:
    app: messaging-app
    version: "blue" # Pointing to Blue initially
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP
